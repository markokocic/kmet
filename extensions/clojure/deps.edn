{:deps {dev.weavejester/cljfmt {:mvn/version "0.16.5"
                        ;; rewrite-clj is bb-bundled (adapted port) — use the
                        ;; bundled copy, never the Maven lib (which requires
                        ;; clojure.tools.reader.impl.* that bb does not ship)
                        :exclusions [rewrite-clj/rewrite-clj]}}}
